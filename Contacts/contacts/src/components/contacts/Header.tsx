function Header(){
    return (
        <div className="Header">
            <h1>Contacts</h1>
        </div>
    );
};

export default Header;

// This ultimately gets exported to App.tsx to generate the info on the screen